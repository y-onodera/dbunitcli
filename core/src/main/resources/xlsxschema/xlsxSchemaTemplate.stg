rowEntry(row) ::= <<
    {
      "sheetName": "$first(row.rows).SHEET_NAME$"
     ,"tableName": "$row.tableName$"
     ,"header": [$row.rows:{r | "$r.COLUMN_NAME$"}; separator=","$]
     ,"dataStart": $first(row.rows).DATA_START$
     ,"columnIndex": [$row.rows:{r | $r.COLUMN_INDEX$}; separator=","$]
     ,"breakKey": [$if(row.dataset.PK.rows)$$row.dataset.PK.rows:{r | "$r.COLUMN_NAME$"}; separator=","$$else$"$first(row.rows).COLUMN_NAME$"$endif$]
    }
>>

cellEntry(row) ::= <<
    {
      "sheetName": "$first(row.dataset.CELLS.rows).SHEET_NAME$"
     ,"tableName": "$row.tableName$"
     ,"header": [$row.dataset.CELLS.rows:{r | "$r.COLUMN_NAME$"}; separator=","$]
     ,"rows": [{"cellAddress": [$row.dataset.CELLS.rows:{r | "$r.CELL_ADDRESS$"}; separator=","$]}]
    }
>>
