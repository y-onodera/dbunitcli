{
  "commonSettings": [
    {
      "string": {
        "COLUMN_SIZE": "COLUMN_SIZE != null && !COLUMN_SIZE.isEmpty() ? COLUMN_SIZE : null",
        "DECIMAL_DIGITS": "DECIMAL_DIGITS != null && !DECIMAL_DIGITS.isEmpty() ? DECIMAL_DIGITS : null"
      },
      "boolean": {
        "NULLABLE": "NULLABLE",
        "IS_PK": "IS_PK"
      },
      "aggregates": [
        {"name": "pkColumnNames", "column": "COLUMN_NAME", "filter": ["IS_PK"]},
        {"name": "pkConstraintName", "column": "PK_NAME", "filter": ["IS_PK"]}
      ]
    }
  ]
}
