# コーディングスタイル規約

## 三項演算子のネスト禁止（Biome `noNestedTernary`）

複雑な条件分岐は if/else を使うこと。JSX内のネスト三項も禁止 — render関数に切り出す。

## デフォルト値には `??` を優先

`undefined`/`null` のみデフォルト適用したい場合は `??`。`||` はfalsy値（0, "", false）もデフォルトに置き換えてしまう。

## JSX内の条件レンダリング

`&&` または1段の三項演算子のみ。複雑な条件は render 関数またはコンポーネントに切り出す。
