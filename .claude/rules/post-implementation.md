# 実装後の手順

**重要**: コードの変更・追加・修正が完了したら、必ず最後に `/simplify` スラッシュコマンドを実行してください。以下のファイルへの変更も `/simplify` の対象とします（サブプロジェクト `core/`, `sidecar/`, `tauri/`, `gui/` 配下を含む）：

- すべての `CLAUDE.md` ファイル
- すべての `.claude/rules/` 配下のファイル（`*.md`）

## 理由

- コードの品質・再利用性・効率性を確認するため
- 一貫したコードスタイルを維持するため
